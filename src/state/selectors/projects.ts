export const selectProjectState = (state) => state.projectsState;

export const selectAllProjects = (state) => state.projectsState.allProjects;

export const selectUserProjects = (state) => state.projectsState.userProjects;
